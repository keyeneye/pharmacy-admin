import type { OrderStatus } from '../types'; // Adjusted path assuming types is sibling to constants

export const statusSortOrder: OrderStatus[] = ["Pending", "Processing", "Shipped", "Delivered", "Cancelled"]; 